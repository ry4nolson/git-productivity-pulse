# Optional — enables the "Sign in with GitHub" button.
# Leave unset to use the personal-access-token flow (still auto-fills username + orgs).
# See oauth/README.md for setup.
VITE_GITHUB_CLIENT_ID=
VITE_OAUTH_PROXY_URL=
